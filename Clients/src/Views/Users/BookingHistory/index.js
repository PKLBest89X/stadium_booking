import React from "react";
import PageLayout from "../../../Components/PageLayout";

const BookingHistory = ({ ...rest }) => {
  return (
    <PageLayout title="ປະຫວັດການຈອງເດີ່ນ" {...rest}>
      <h1>ປະຫວັດການຈອງເດີ່ນ</h1>
    </PageLayout>
  );
};

export default BookingHistory;
